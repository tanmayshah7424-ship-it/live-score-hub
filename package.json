{
    "name": "scorehub-live-root",
    "version": "1.0.0",
    "description": "Root package for ScoreHub Live",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && npm install --prefix server && npm install --prefix frontend",
        "build": "npm install --prefix server && npm install --prefix frontend && npm run build --prefix frontend",
        "start": "node server/server.js"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "axios": "^1.13.5"
    }
}